fastapi==0.128.8
html5lib==1.1
lxml==6.0.4
numpy==2.4.4
pandas==3.0.2
requests==2.33.1
uvicorn==0.40.0
yfinance==1.2.2

# Test deps (FastAPI TestClient needs httpx)
httpx==0.28.1
pytest==9.0.3

# Vision tagging pipeline (installed only where /api/vision/run is used)
# Roboflow's SDK is preferred when available; as of 1.3.3 it supports Python <3.13.
# The backend falls back to the hosted REST endpoint on newer Python versions.
inference-sdk==1.3.3; python_version < "3.13"
opencv-python-headless==4.13.0.92
playwright==1.61.0
supervision==0.29.1
